---
layout: singlePage
---

<article class="post-page">
  <header class="page-hero compact">
    <p class="section-kicker">{{ page.date | date: "%B %e, %Y" }}</p>
    <h1>{{ page.title }}</h1>
  </header>

  {{ content }}
</article>
